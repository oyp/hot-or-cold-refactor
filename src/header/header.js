import React from 'react';

import NavBar from './navbar';

import './header.css';

export default function Header(props) {
    return (
        <header>
            <NavBar />
            <h1></h1>
        </header>
    );
}